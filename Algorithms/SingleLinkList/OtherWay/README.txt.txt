This way use two struct. One for data and one for list
The struct for data is normal
The struct for list has two data' pointer. One is begin pointer and one is end pointer

So functions of this way are diffrent 